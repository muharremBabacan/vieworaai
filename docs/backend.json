{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the Viewora AI Coach application, including their economic and progression state.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the user entity."
        },
        "email": {
          "type": "string",
          "description": "User's email address.",
          "format": "email"
        },
        "name": {
          "type": "string",
          "description": "User's full name."
        },
        "aura_balance": {
          "type": "number",
          "description": "User's current balance of Aura, the in-app currency."
        },
        "current_xp": {
          "type": "number",
          "description": "User's current experience points."
        },
        "level_name": {
          "type": "string",
          "description": "User's current level name in the gamification system (e.g., Neuner, Vexer)."
        },
        "is_mentor": {
            "type": "boolean",
            "description": "Flag that indicates if the user has reached the 'Vexer' level and gained mentor status."
        },
        "weekly_free_refill_date": {
            "type": "string",
            "format": "date-time",
            "description": "Timestamp tracking the last weekly free Aura refill."
        },
        "completed_modules": {
            "type": "array",
            "description": "An array of IDs for the academy modules the user has completed.",
            "items": {
                "type": "string"
            }
        },
        "interests": {
          "type": "array",
          "description": "User's selected photography interests.",
          "items": {
            "type": "string"
          }
        },
        "onboarded": {
          "type": "boolean",
          "description": "Flag to check if the user has completed the onboarding process."
        }
      },
      "required": [
        "id",
        "email",
        "name",
        "aura_balance",
        "current_xp",
        "level_name",
        "onboarded",
        "completed_modules",
        "weekly_free_refill_date"
      ]
    },
    "Photo": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Photo",
      "type": "object",
      "description": "Represents a photo analyzed by the AI Coach.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the photo entity."
        },
        "imageURL": {
          "type": "string",
          "description": "URL of the image."
        },
        "aiFeedback": {
          "type": "string",
          "description": "AI feedback on the photo."
        },
        "userId": {
          "type": "string",
          "description": "Reference to User. (Relationship: User 1:N Photo)"
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp of when the photo was analyzed.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "imageURL",
        "aiFeedback",
        "userId",
        "createdAt"
      ]
    },
    "Lesson": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Lesson",
      "type": "object",
      "description": "Represents a lesson in the Viewora Academy.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the lesson entity."
        },
        "category": {
          "type": "string",
          "description": "Category of the lesson (e.g., Composition, Lighting)."
        },
        "title": {
          "type": "string",
          "description": "Title of the lesson."
        },
        "content": {
          "type": "string",
          "description": "Content of the lesson."
        },
        "imageURL": {
          "type": "string",
          "description": "URL of the image associated with the lesson."
        }
      },
      "required": [
        "id",
        "category",
        "title",
        "content",
        "imageURL"
      ]
    },
    "Transaction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Transaction",
      "type": "object",
      "description": "Represents a transaction (purchase, gift, or refill) of Aura.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the transaction entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to User. (Relationship: User 1:N Transaction)"
        },
        "amount": {
          "type": "number",
          "description": "Amount of Aura involved in the transaction."
        },
        "type": {
          "type": "string",
          "description": "Type of transaction (Purchase, Gift, Refill)."
        },
        "status": {
          "type": "string",
          "description": "Status of the transaction."
        },
        "transactionDate": {
          "type": "string",
          "description": "Timestamp of when the transaction occured.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "amount",
        "type",
        "status",
        "transactionDate"
      ]
    }
  },
  "auth": {
    "providers": [
      "google.com"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores user profiles. Accessible only by the user. Uses path-based ownership.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/photos/{photoId}",
        "definition": {
          "entityName": "Photo",
          "schema": {
            "$ref": "#/backend/entities/Photo"
          },
          "description": "Stores photos analyzed by the AI Coach, associated with a specific user. Includes denormalized 'userId' for authorization independence.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            },
            {
              "name": "photoId",
              "description": "The unique identifier for the photo."
            }
          ]
        }
      },
      {
        "path": "/lessons/{lessonId}",
        "definition": {
          "entityName": "Lesson",
          "schema": {
            "$ref": "#/backend/entities/Lesson"
          },
          "description": "Stores lessons in the Viewora Academy. Publicly accessible.",
          "params": [
            {
              "name": "lessonId",
              "description": "The unique identifier for the lesson."
            }
          ]
        }
      },
      {
        "path": "/users/{userId}/transactions/{transactionId}",
        "definition": {
          "entityName": "Transaction",
          "schema": {
            "$ref": "#/backend/entities/Transaction"
          },
          "description": "Stores transaction records for each user. Includes denormalized 'userId' for authorization independence.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier for the user."
            },
            {
              "name": "transactionId",
              "description": "The unique identifier for the transaction."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore data structure is designed to support the Viewora AI Coach application's features, focusing on secure access, scalability, and maintainability. The key principle is Authorization Independence, achieved through denormalization. The structure is segregated to provide homogeneous security postures across collections.\n\n*   **Users (Private Data):** User data is stored under `/users/{userId}` ensuring only the authenticated user can access their own data. Path-based ownership simplifies security rules.\n*   **Photos (User-Owned Data):** Photos analyzed by the AI are stored under `/users/{userId}/photos/{photoId}`. This hierarchical structure maintains the 1:N relationship between Users and Photos. The `userId` is denormalized into the `Photo` document to enforce authorization independence.\n*   **Lessons (Public Data):** Lessons are stored in the `/lessons/{lessonId}` collection.  These are globally accessible so no user-specific authorization is applied.\n*   **Transactions (User-Owned Data):** Transaction data is stored under `/users/{userId}/transactions/{transactionId}`. This structure allows tracking of each user's transactions and follows the 1:N relationship between Users and Transactions. The `userId` is denormalized into the `Transaction` document to enforce authorization independence.\n\nThis design supports the required QAPs by:\n\n1.  **Authorization Independence:** Denormalizing `userId` into the `photos` and `transactions` collections eliminates the need for `get()` calls in security rules, enabling atomic operations.\n2.  **QAPs (Rules are not Filters):**  The structure enables secure `list` operations. Listing photos is secure as all photos under `/users/{userId}/photos/{photoId}` belong to the authenticated user. Similarly, listing transactions is secure as all transactions under `/users/{userId}/transactions/{transactionId}` belong to the authenticated user.\n3.  **Clarity of Intent (Debuggability):** The structure makes authorization intent explicit.  The use of path-based ownership for user-specific data clearly indicates that access is controlled by the user's ID."
  }
}
