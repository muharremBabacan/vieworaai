{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account in the Viewora AI Coach application, storing their profile information, token balance, and gamification level.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The user's email address, used for identification and communication.",
          "format": "email"
        },
        "name": {
          "type": "string",
          "description": "The user's display name."
        },
        "tokenBalance": {
          "type": "number",
          "description": "The current number of tokens available to the user for AI analysis."
        },
        "planLevel": {
          "type": "string",
          "description": "The user's current gamification level (e.g., Basic, Mid, Pro)."
        },
        "xp": {
          "type": "number",
          "description": "Experience points earned by the user, contributing to their gamification level."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user account was created.",
          "format": "date-time"
        },
        "lastLoginAt": {
          "type": "string",
          "description": "Timestamp of the user's last login.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "email",
        "name",
        "tokenBalance",
        "planLevel",
        "xp",
        "createdAt"
      ]
    },
    "Photo": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Photo",
      "type": "object",
      "description": "Represents a photo submitted by a user for AI analysis, including the image URL and the AI's feedback.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Photo entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who submitted this photo. (Relationship: User 1:N Photo)"
        },
        "imageUrl": {
          "type": "string",
          "description": "The URL of the photo image.",
          "format": "uri"
        },
        "aiFeedback": {
          "type": "string",
          "description": "The AI-generated analysis and improvement tips for the photo, covering aspects like Light, Composition, Emotion, and Technique. This is stored as a string, potentially containing formatted text or JSON."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the photo was submitted and analyzed.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "imageUrl",
        "aiFeedback",
        "createdAt"
      ]
    },
    "AcademyLesson": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AcademyLesson",
      "type": "object",
      "description": "Represents a single micro-learning lesson within the Viewora Academy, providing educational content to users.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AcademyLesson entity."
        },
        "category": {
          "type": "string",
          "description": "The category of the lesson (e.g., 'Kompozisyon', 'Işık', 'Teknik')."
        },
        "title": {
          "type": "string",
          "description": "The title of the lesson."
        },
        "content": {
          "type": "string",
          "description": "The main educational content or technical tip for the lesson."
        },
        "imageUrl": {
          "type": "string",
          "description": "The URL of an example photograph associated with the lesson.",
          "format": "uri"
        }
      },
      "required": [
        "id",
        "category",
        "title",
        "content",
        "imageUrl"
      ]
    },
    "UserLessonProgress": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserLessonProgress",
      "type": "object",
      "description": "Tracks a user's progress through the Viewora Academy lessons, recording which lessons they have completed.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserLessonProgress entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User making progress. (Relationship: User 1:N UserLessonProgress)"
        },
        "lessonId": {
          "type": "string",
          "description": "Reference to the AcademyLesson being tracked. (Relationship: AcademyLesson 1:N UserLessonProgress)"
        },
        "isCompleted": {
          "type": "boolean",
          "description": "Indicates whether the user has marked this lesson as completed."
        },
        "completedAt": {
          "type": "string",
          "description": "Timestamp when the lesson was marked as completed by the user.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "lessonId",
        "isCompleted"
      ]
    },
    "Transaction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Transaction",
      "type": "object",
      "description": "Records financial and token-related transactions, such as token purchases, gifts, or token spending for AI analysis.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Transaction entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User involved in the transaction. (Relationship: User 1:N Transaction)"
        },
        "amount": {
          "type": "number",
          "description": "The number of tokens involved in this transaction (positive for gain, negative for spend)."
        },
        "currencyAmount": {
          "type": "number",
          "description": "The monetary amount of the transaction, if applicable (e.g., for purchases)."
        },
        "currency": {
          "type": "string",
          "description": "The currency used for the transaction, if applicable (e.g., 'TL')."
        },
        "type": {
          "type": "string",
          "description": "The type of transaction (e.g., 'Purchase', 'Gift', 'Analysis_Spend')."
        },
        "status": {
          "type": "string",
          "description": "The status of the transaction (e.g., 'Pending', 'Completed', 'Failed')."
        },
        "transactionDate": {
          "type": "string",
          "description": "Timestamp when the transaction occurred.",
          "format": "date-time"
        },
        "paymentProviderTransactionId": {
          "type": "string",
          "description": "External transaction ID from the payment provider (e.g., Iyzico/Stripe), if applicable."
        }
      },
      "required": [
        "id",
        "userId",
        "amount",
        "type",
        "status",
        "transactionDate"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles including authentication details, token balance, and gamification level. This collection is private, with documents owned by the user matching the '{userId}' wildcard.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user, matching 'request.auth.uid'."
            }
          ]
        }
      },
      {
        "path": "users/{userId}/photos/{photoId}",
        "definition": {
          "entityName": "Photo",
          "schema": {
            "$ref": "#/backend/entities/Photo"
          },
          "description": "A subcollection storing photos submitted by a specific user for AI analysis. Documents are private and owned by the user matching the '{userId}' wildcard. Includes denormalized 'userId' for authorization independence and easier querying.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns these photos."
            },
            {
              "name": "photoId",
              "description": "The unique identifier for a specific photo."
            }
          ]
        }
      },
      {
        "path": "academyLessons/{lessonId}",
        "definition": {
          "entityName": "AcademyLesson",
          "schema": {
            "$ref": "#/backend/entities/AcademyLesson"
          },
          "description": "A top-level collection containing all micro-learning lessons for the Viewora Academy. These documents are public and read-only for all authenticated users.",
          "params": [
            {
              "name": "lessonId",
              "description": "The unique identifier for an academy lesson."
            }
          ]
        }
      },
      {
        "path": "users/{userId}/lessonProgress/{progressId}",
        "definition": {
          "entityName": "UserLessonProgress",
          "schema": {
            "$ref": "#/backend/entities/UserLessonProgress"
          },
          "description": "A subcollection tracking a specific user's progress through the academy lessons. Documents are private and owned by the user matching the '{userId}' wildcard. Includes denormalized 'userId' and 'lessonId' for authorization independence and easier querying.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user whose lesson progress is being tracked."
            },
            {
              "name": "progressId",
              "description": "The unique identifier for a specific user's lesson progress entry."
            }
          ]
        }
      },
      {
        "path": "users/{userId}/transactions/{transactionId}",
        "definition": {
          "entityName": "Transaction",
          "schema": {
            "$ref": "#/backend/entities/Transaction"
          },
          "description": "A subcollection recording all financial and token-related transactions for a specific user. Documents are private and owned by the user matching the '{userId}' wildcard. Includes denormalized 'userId' for authorization independence and easier querying.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user involved in the transaction."
            },
            {
              "name": "transactionId",
              "description": "The unique identifier for a specific transaction."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore data structure for Viewora AI Coach is designed with a strong emphasis on Authorization Independence, Structural Segregation, and predictable access patterns to ensure security, scalability, and debuggability. The core strategy revolves around path-based authorization and denormalization.\n\n**Authorization Independence:** This is achieved primarily by structuring all user-specific data as subcollections under a top-level `/users/{userId}` collection. For instance, user photos are stored at `/users/{userId}/photos/{photoId}`, user lesson progress at `/users/{userId}/lessonProgress/{progressId}`, and transactions at `/users/{userId}/transactions/{transactionId}`. This design ensures that security rules can directly compare `request.auth.uid` with the `{userId}` wildcard in the path, eliminating the need for expensive and potentially insecure `get()` calls to parent documents to determine ownership or permissions. Each document within these subcollections also includes the `userId` field as a form of denormalization, which aids in querying and provides an additional layer of validation, although the path remains the primary authorization vector.\n\n**QAPs (Queries as Authorization Primitives):** The structural segregation directly supports secure `list` operations. Because all documents in a path like `/users/{userId}/photos` inherently belong to the `userId` specified in the path, any `list` query on this path will only return documents that the authenticated user (`request.auth.uid`) is authorized to access. This prevents rules from being used as filters, which is inefficient and insecure. Similarly, the `academyLessons` collection is designed as a top-level, public read-only collection (`/academyLessons/{lessonId}`). This clear segregation of public vs. private data means `list` operations on `/academyLessons` are securely accessible to all authenticated users for reading, without needing complex filtering logic.\n\n**Structural Segregation:** Data with different security postures is kept in separate collections or distinct hierarchical paths. For example, `AcademyLesson` entities, which are globally accessible (read-only for all authenticated users), reside in a top-level collection `/academyLessons`. In contrast, `Photo`, `UserLessonProgress`, and `Transaction` entities, which are strictly owned by individual users, are nested under their respective user paths, ensuring their private nature. There is no mixing of public and private data within the same collection, simplifying security rule logic.\n\n**Access Modeling:** The design consistently uses hierarchical paths for user-owned data, such as `/users/{userId}/photos`, making ownership explicit and rules straightforward. Global roles, if needed, would leverage existence checks in dedicated collections (e.g., `/roles_admin/{uid}`), adhering to DBAC principles. Currently, no collaborative data requiring membership maps is identified.\n\n**Data Clarity and Predictability:** Field names like `userId`, `photoId`, `lessonId`, etc., are semantically consistent, and wildcards are descriptive. The `Transaction` entity utilizes an explicit `status` field for state modeling. The provided schemas ensure predictable data structures.\n\n**Regarding the 'auth/requests-to-this-api...' error:** This error is related to Firebase Authentication setup and API key configurations, specifically concerning the Identity Toolkit API. It is outside the scope of Firestore data structure design. The proposed Firestore structure assumes that Firebase Authentication is correctly enabled and configured in the Google Cloud project to allow these API calls for user authentication to succeed."
  }
}