
{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account in the Viewora AI Coach application, storing their profile information, token balance, and gamification level.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The user's email address, used for identification and communication.",
          "format": "email"
        },
        "name": {
          "type": "string",
          "description": "The user's display name."
        },
        "photoURL": {
          "type": "string",
          "description": "URL of the user's profile picture.",
          "format": "uri"
        },
        "auro_balance": {
          "type": "number",
          "description": "The current number of tokens available to the user for AI analysis."
        },
        "level_name": {
          "type": "string",
          "description": "The user's current gamification level (e.g., Basic, Mid, Pro)."
        },
        "current_xp": {
          "type": "number",
          "description": "Experience points earned by the user, contributing to their gamification level."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user account was created.",
          "format": "date-time"
        },
        "lastLoginAt": {
          "type": "string",
          "description": "Timestamp of the user's last login.",
          "format": "date-time"
        },
        "communication_style": {
          "type": "string",
          "description": "User's preferred communication style for AI feedback.",
          "enum": ["soft", "balanced", "technical"]
        },
        "score_history": {
          "type": "array",
          "description": "A recent history of analysis scores to determine trends.",
          "items": {
            "type": "object",
            "properties": {
              "score": { "type": "number" },
              "date": { "type": "string", "format": "date-time" }
            }
          }
        }
      },
      "required": [
        "id",
        "email",
        "name",
        "auro_balance",
        "level_name",
        "current_xp",
        "createdAt"
      ]
    },
    "PublicUserProfile": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Public User Profile",
      "type": "object",
      "description": "Stores non-sensitive user information that can be displayed publicly, such as in photo galleries or leaderboards.",
      "properties": {
        "name": {
          "type": "string",
          "description": "The user's display name."
        },
        "photoURL": {
          "type": "string",
          "description": "URL of the user's profile picture.",
          "format": "uri"
        },
        "level_name": {
          "type": "string",
          "description": "The user's current gamification level (e.g., Basic, Mid, Pro)."
        }
      },
      "required": ["name", "level_name"]
    },
    "Photo": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Photo",
      "type": "object",
      "description": "Represents a photo submitted by a user for AI analysis, including the image URL and the AI's feedback.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Photo entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who submitted this photo. (Relationship: User 1:N Photo)"
        },
        "imageUrl": {
          "type": "string",
          "description": "The URL of the photo image.",
          "format": "uri"
        },
        "filePath": {
          "type": "string",
          "description": "The path to the photo file in Firebase Storage."
        },
        "aiFeedback": {
          "type": "object",
          "description": "The raw JSON data from the AI analysis.",
          "properties": {
            "device_estimation": { "type": "string" },
            "genre": { "type": "string" },
            "light_score": { "type": "number" },
            "composition_score": { "type": "number" },
            "focus_score": { "type": "number" },
            "color_control_score": { "type": "number" },
            "background_control_score": { "type": "number" },
            "creativity_risk_score": { "type": "number" },
            "technical_level_estimation": { "type": "string" },
            "error_flags": {
              "type": "object",
              "properties": {
                "overexposed": { "type": "boolean" },
                "underexposed": { "type": "boolean" },
                "cluttered_background": { "type": "boolean" },
                "weak_subject_isolation": { "type": "boolean" },
                "horizon_misalignment": { "type": "boolean" }
              }
            },
            "short_neutral_analysis": { "type": "string" }
          }
        },
        "adaptiveFeedback": {
            "type": "string",
            "description": "The human-readable adaptive feedback generated based on the user's level and the photo's analysis data."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the photo was submitted and analyzed.",
          "format": "date-time"
        },
        "userName": {
          "type": "string",
          "description": "Denormalized user name for public display."
        },
        "userPhotoURL": {
          "type": "string",
          "format": "uri",
          "description": "Denormalized user profile picture URL."
        },
        "userLevelName": {
          "type": "string",
          "description": "Denormalized user level name."
        },
        "likes": {
          "type": "array",
          "description": "A list of user UIDs who have liked this photo.",
          "items": { "type": "string" }
        }
      },
      "required": [
        "id",
        "userId",
        "imageUrl"
      ]
    },
    "AcademyLesson": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AcademyLesson",
      "type": "object",
      "description": "Represents a single micro-learning lesson within the Viewora Academy, providing educational content to users.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AcademyLesson entity."
        },
        "level": {
          "type": "string",
          "description": "The difficulty level of the lesson. One of: 'Temel', 'Orta', 'İleri'."
        },
        "category": {
          "type": "string",
          "description": "The specific topic/category of the lesson from the curriculum (e.g., 'Pozlama Temelleri', 'Görsel Hikâye Anlatımı')."
        },
        "title": {
          "type": "string",
          "description": "The title of the lesson."
        },
        "learningObjective": {
          "type": "string",
          "description": "The skill the user will gain from this lesson."
        },
        "theory": {
          "type": "string",
          "description": "The main educational content/theory for the lesson."
        },
        "analysisCriteria": {
          "type": "array",
          "description": "A list of 3 technical criteria for a photo to be considered 'successful' according to this lesson.",
          "items": {
            "type": "string"
          }
        },
        "practiceTask": {
          "type": "string",
          "description": "A specific, actionable shooting task for the user."
        },
        "auroNote": {
          "type": "string",
          "description": "An explanation of why this lesson's analysis provides a professional perspective."
        },
        "imageUrl": {
          "type": "string",
          "description": "The URL of an example photograph associated with the lesson.",
          "format": "uri"
        },
        "imageHint": {
          "type": "string",
          "description": "Keywords used to find a suitable image for the lesson."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the lesson was generated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "level",
        "category",
        "title",
        "learningObjective",
        "theory",
        "analysisCriteria",
        "practiceTask",
        "auroNote",
        "imageUrl",
        "imageHint",
        "createdAt"
      ]
    },
    "UserLessonProgress": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserLessonProgress",
      "type": "object",
      "description": "Tracks a user's progress through the Viewora Academy lessons, recording which lessons they have completed.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserLessonProgress entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User making progress. (Relationship: User 1:N UserLessonProgress)"
        },
        "lessonId": {
          "type": "string",
          "description": "Reference to the AcademyLesson being tracked. (Relationship: AcademyLesson 1:N UserLessonProgress)"
        },
        "isCompleted": {
          "type": "boolean",
          "description": "Indicates whether the user has marked this lesson as completed."
        },
        "completedAt": {
          "type": "string",
          "description": "Timestamp when the lesson was marked as completed by the user.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "lessonId",
        "isCompleted"
      ]
    },
    "Transaction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Transaction",
      "type": "object",
      "description": "Records financial and token-related transactions, such as token purchases, gifts, or token spending for AI analysis.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Transaction entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User involved in the transaction. (Relationship: User 1:N Transaction)"
        },
        "amount": {
          "type": "number",
          "description": "The number of tokens involved in this transaction (positive for gain, negative for spend)."
        },
        "currencyAmount": {
          "type": "number",
          "description": "The monetary amount of the transaction, if applicable (e.g., for purchases)."
        },
        "currency": {
          "type": "string",
          "description": "The currency used for the transaction, if applicable (e.g., 'TL')."
        },
        "type": {
          "type": "string",
          "description": "The type of transaction (e.g., 'Purchase', 'Gift', 'Analysis_Spend')."
        },
        "status": {
          "type": "string",
          "description": "The status of the transaction (e.g., 'Pending', 'Completed', 'Failed')."
        },
        "transactionDate": {
          "type": "string",
          "description": "Timestamp when the transaction occurred.",
          "format": "date-time"
        },
        "paymentProviderTransactionId": {
          "type": "string",
          "description": "External transaction ID from the payment provider (e.g., Iyzico/Stripe), if applicable."
        }
      },
      "required": [
        "id",
        "userId",
        "amount",
        "type",
        "status",
        "transactionDate"
      ]
    },
    "Group": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Group",
      "type": "object",
      "description": "Represents a user-created community, class, or workshop within the Viewora application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Group entity."
        },
        "name": {
          "type": "string",
          "description": "The display name of the group."
        },
        "description": {
          "type": "string",
          "description": "A short description of the group's purpose or theme."
        },
        "ownerId": {
          "type": "string",
          "description": "The UID of the user who created and owns the group."
        },
        "memberIds": {
          "type": "array",
          "description": "A list of user UIDs who are members of this group.",
          "items": {
            "type": "string"
          }
        },
        "maxMembers": {
          "type": "number",
          "description": "The maximum number of members this group can have, based on the owner's level."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the group was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "ownerId",
        "memberIds",
        "maxMembers",
        "createdAt"
      ]
    },
    "GroupInvite": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "GroupInvite",
      "type": "object",
      "description": "Represents a pending invitation for a user to join a group. This is a central collection.",
      "properties": {
        "id": { "type": "string" },
        "groupId": { "type": "string" },
        "groupName": { "type": "string", "description": "Denormalized for easy display in notifications." },
        "fromUserId": { "type": "string" },
        "fromUserName": { "type": "string", "description": "Denormalized for easy display." },
        "toUserId": { "type": "string", "description": "The UID of the user who is invited." },
        "status": { "type": "string", "enum": ["pending", "accepted", "declined"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["groupId", "groupName", "fromUserId", "fromUserName", "toUserId", "status", "createdAt"]
    },
    "GroupAssignment": {
      "title": "Group Assignment",
      "type": "object",
      "description": "An assignment or task given by a group owner to the members.",
      "properties": {
        "id": { "type": "string" },
        "groupId": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "dueDate": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["groupId", "title", "dueDate", "createdAt"]
    },
    "AssignmentSubmission": {
      "title": "Assignment Submission",
      "type": "object",
      "description": "A member's submission for a group assignment.",
      "properties": {
        "id": { "type": "string" },
        "assignmentId": { "type": "string" },
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "photoUrl": { "type": "string", "format": "uri" },
        "filePath": { "type": "string" },
        "submittedAt": { "type": "string", "format": "date-time" }
      },
      "required": ["assignmentId", "userId", "userName", "photoUrl", "submittedAt"]
    },
    "GroupEvent": {
      "title": "Group Event",
      "type": "object",
      "description": "An event or trip planned for a group.",
      "properties": {
        "id": { "type": "string" },
        "groupId": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "location": { "type": "string" },
        "eventDate": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" },
        "rsvps": {
          "type": "object",
          "description": "A map of user IDs to their RSVP status ('attending', 'declined').",
          "additionalProperties": { "type": "string", "enum": ["attending", "declined"] }
        }
      },
      "required": ["groupId", "title", "eventDate", "createdAt"]
    },
    "Competition": {
      "title": "Competition",
      "type": "object",
      "description": "A global competition organized by Viewora admins.",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "theme": { "type": "string" },
        "prize": { "type": "string" },
        "startDate": { "type": "string", "format": "date-time" },
        "endDate": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" },
        "imageUrl": { "type": "string", "format": "uri" },
        "imageHint": { "type": "string" }
      },
      "required": ["title", "description", "theme", "prize", "startDate", "endDate", "createdAt", "imageUrl"]
    },
    "GroupCompetition": {
      "title": "Group Competition",
      "type": "object",
      "description": "A competition held within a group.",
      "properties": {
        "id": { "type": "string" },
        "groupId": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "theme": { "type": "string" },
        "prize": { "type": "string" },
        "startDate": { "type": "string", "format": "date-time" },
        "endDate": { "type": "string", "format": "date-time" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["groupId", "title", "startDate", "endDate", "createdAt"]
    },
    "CompetitionEntry": {
      "title": "Competition Entry",
      "type": "object",
      "description": "A member's entry for a group competition.",
      "properties": {
        "id": { "type": "string" },
        "competitionId": { "type": "string" },
        "userId": { "type": "string" },
        "userName": { "type": "string" },
        "photoUrl": { "type": "string", "format": "uri" },
        "filePath": { "type": "string" },
        "submittedAt": { "type": "string", "format": "date-time" },
        "votes": { "type": "array", "items": { "type": "string" } }
      },
      "required": ["competitionId", "userId", "userName", "photoUrl", "submittedAt"]
    },
    "GroupTraining": {
      "title": "Group Training",
      "type": "object",
      "description": "A training or educational material assigned to a group.",
      "properties": {
        "id": { "type": "string" },
        "groupId": { "type": "string" },
        "title": { "type": "string" },
        "description": { "type": "string" },
        "linkedLessonId": { "type": "string", "description": "Optional ID of a lesson from the main Academy." },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["groupId", "title", "createdAt"]
    },
    "UserProfileIndex": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User Profile Index",
      "type": "object",
      "description": "An AI-generated summary of a user's photographic style, strengths, and areas for improvement based on their recent work.",
      "properties": {
        "userId": {
          "type": "string",
          "description": "The ID of the user this profile index belongs to."
        },
        "dominant_style": {
          "type": "string",
          "description": "The most frequent photography genre identified in the user's recent photos (e.g., 'portrait', 'landscape')."
        },
        "strengths": {
          "type": "array",
          "description": "A list of the user's most consistent strengths (e.g., 'composition', 'lighting').",
          "items": { "type": "string" }
        },
        "areas_for_improvement": {
          "type": "array",
          "description": "A list of areas where the user could improve (e.g., 'focus control', 'background simplicity').",
          "items": { "type": "string" }
        },
        "trend": {
          "type": "string",
          "enum": ["improving", "stagnant", "declining"],
          "description": "The user's performance trend based on recent analysis scores."
        },
        "last_updated": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of when this index was last updated."
        }
      },
      "required": [
        "userId",
        "dominant_style",
        "strengths",
        "areas_for_improvement",
        "trend",
        "last_updated"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "users/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/User"
      },
      "description": "Stores individual user profiles including authentication details, token balance, and gamification level. This collection is private, with documents owned by the user matching the '{userId}' wildcard."
    },
    "public_profiles/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/PublicUserProfile"
      },
      "description": "Stores public-facing, non-sensitive user profile data. Readable by all authenticated users, writable only by the owner."
    },
    "users/{userId}/photos/{photoId}": {
      "schema": {
        "$ref": "#/backend/entities/Photo"
      },
      "description": "A subcollection storing photos submitted by a specific user for AI analysis. Documents are private and owned by the user matching the '{userId}' wildcard. Includes denormalized 'userId' for authorization independence and easier querying."
    },
    "users/{userId}/lessonProgress/{progressId}": {
      "schema": {
        "$ref": "#/backend/entities/UserLessonProgress"
      },
      "description": "A subcollection tracking a specific user's progress through the academy lessons. Documents are private and owned by the user matching the '{userId}' wildcard. Includes denormalized 'userId' and 'lessonId' for authorization independence and easier querying."
    },
    "users/{userId}/transactions/{transactionId}": {
      "schema": {
        "$ref": "#/backend/entities/Transaction"
      },
      "description": "A subcollection recording all financial and token-related transactions for a specific user. Documents are private and owned by the user matching the '{userId}' wildcard. Includes denormalized 'userId' for authorization independence and easier querying."
    },
    "groups/{groupId}": {
      "schema": {
        "$ref": "#/backend/entities/Group"
      },
      "description": "Stores group information, including owner and member lists. Access is restricted to members."
    },
    "groups/{groupId}/photos/{photoId}": {
      "schema": {
        "$ref": "#/backend/entities/Photo"
      },
      "description": "A subcollection for photos shared within a specific group. Access is limited to group members."
    },
    "group_invites/{inviteId}": {
      "schema": {
        "$ref": "#/backend/entities/GroupInvite"
      },
      "description": "A global collection of all pending and processed group invitations."
    },
    "academyLessons/{lessonId}": {
      "schema": {
        "$ref": "#/backend/entities/AcademyLesson"
      },
      "description": "A top-level collection containing all micro-learning lessons for the Viewora Academy. These documents are public and read-only for all authenticated users."
    },
    "groups/{groupId}/assignments/{assignmentId}": {
      "schema": {
        "$ref": "#/backend/entities/GroupAssignment"
      },
      "description": "Stores assignments for a specific group. Writable only by group owner."
    },
    "groups/{groupId}/assignments/{assignmentId}/submissions/{submissionId}": {
      "schema": {
        "$ref": "#/backend/entities/AssignmentSubmission"
      },
      "description": "Stores member submissions for a specific assignment. Writable only by the submitting member."
    },
    "groups/{groupId}/events/{eventId}": {
      "schema": {
        "$ref": "#/backend/entities/GroupEvent"
      },
      "description": "Stores events (trips, meetings) for a specific group. Writable by owner, but members can update their own RSVP status."
    },
    "competitions/{competitionId}": {
      "schema": {
        "$ref": "#/backend/entities/Competition"
      },
      "description": "Stores global competitions created by admins. Readable by all authenticated users, writable only by admins."
    },
    "competitions/{competitionId}/entries/{entryId}": {
      "schema": {
        "$ref": "#/backend/entities/CompetitionEntry"
      },
      "description": "Stores user submissions for a specific global competition."
    },
    "groups/{groupId}/competitions/{competitionId}": {
      "schema": {
        "$ref": "#/backend/entities/GroupCompetition"
      },
      "description": "Stores competitions for a specific group. Writable only by group owner."
    },
    "groups/{groupId}/competitions/{competitionId}/entries/{entryId}": {
      "schema": {
        "$ref": "#/backend/entities/CompetitionEntry"
      },
      "description": "Stores member entries for a specific competition. Writable only by the submitting member, members can vote."
    },
    "groups/{groupId}/trainings/{trainingId}": {
      "schema": {
        "$ref": "#/backend/entities/GroupTraining"
      },
      "description": "Stores trainings for a specific group. Writable only by group owner."
    },
    "user_profile_index/{userId}": {
      "schema": {
        "$ref": "#/backend/entities/UserProfileIndex"
      },
      "description": "Stores the AI-generated photographic profile index for each user. Documents are private and owned by the user matching the '{userId}'."
    },
    "public_photos/{photoId}": {
      "schema": {
        "$ref": "#/backend/entities/Photo"
      },
      "description": "Stores public-facing photos submitted to the general exhibition hall. Readable by all authenticated users."
    }
  }
}
