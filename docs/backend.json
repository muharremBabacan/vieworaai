{
  "entities": {
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user account in the Viewora AI Coach application, storing their profile information, token balance, and gamification level.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the User entity."
        },
        "email": {
          "type": "string",
          "description": "The user's email address, used for identification and communication.",
          "format": "email"
        },
        "name": {
          "type": "string",
          "description": "The user's display name."
        },
        "auro_balance": {
          "type": "number",
          "description": "The current number of tokens available to the user for AI analysis."
        },
        "level_name": {
          "type": "string",
          "description": "The user's current gamification level (e.g., Basic, Mid, Pro)."
        },
        "current_xp": {
          "type": "number",
          "description": "Experience points earned by the user, contributing to their gamification level."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the user account was created.",
          "format": "date-time"
        },
        "lastLoginAt": {
          "type": "string",
          "description": "Timestamp of the user's last login.",
          "format": "date-time"
        },
        "profileIndex": {
          "$ref": "#/backend/entities/UserProfileIndex",
          "description": "The user's calculated photographic profile index. This is calculated on-demand."
        }
      },
      "required": [
        "id",
        "email",
        "name",
        "auro_balance",
        "level_name",
        "current_xp",
        "createdAt"
      ]
    },
     "UserProfileIndex": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User Profile Index",
      "type": "object",
      "description": "Stores a calculated index of a user's photographic profile based on their best work.",
      "properties": {
        "overall_profile_score": { "type": "number" },
        "technical_score": { "type": "number" },
        "progress_score": { "type": "number" },
        "activity_score": { "type": "number" },
        "dominant_genre": { "type": "string" },
        "dominant_device": { "type": "string" },
        "last_calculated_at": { "type": "string", "format": "date-time" }
      },
      "required": ["overall_profile_score", "technical_score", "last_calculated_at"]
    },
    "Photo": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Photo",
      "type": "object",
      "description": "Represents a photo submitted by a user for AI analysis, including the image URL and the AI's feedback.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Photo entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User who submitted this photo. (Relationship: User 1:N Photo)"
        },
        "imageUrl": {
          "type": "string",
          "description": "The URL of the photo image.",
          "format": "uri"
        },
        "filePath": {
          "type": "string",
          "description": "The path to the photo file in Firebase Storage."
        },
        "aiFeedback": {
          "type": "object",
          "description": "The raw JSON data from the AI analysis.",
          "properties": {
            "device_estimation": { "type": "string" },
            "genre": { "type": "string" },
            "light_score": { "type": "number" },
            "composition_score": { "type": "number" },
            "focus_score": { "type": "number" },
            "color_control_score": { "type": "number" },
            "background_control_score": { "type": "number" },
            "creativity_risk_score": { "type": "number" },
            "technical_level_estimation": { "type": "string" },
            "error_flags": {
              "type": "object",
              "properties": {
                "overexposed": { "type": "boolean" },
                "underexposed": { "type": "boolean" },
                "cluttered_background": { "type": "boolean" },
                "weak_subject_isolation": { "type": "boolean" },
                "horizon_misalignment": { "type": "boolean" }
              }
            },
            "short_neutral_analysis": { "type": "string" }
          }
        },
        "adaptiveFeedback": {
            "type": "string",
            "description": "The human-readable adaptive feedback generated based on the user's level and the photo's analysis data."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the photo was submitted and analyzed.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "imageUrl"
      ]
    },
    "AcademyLesson": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "AcademyLesson",
      "type": "object",
      "description": "Represents a single micro-learning lesson within the Viewora Academy, providing educational content to users.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the AcademyLesson entity."
        },
        "level": {
          "type": "string",
          "description": "The difficulty level of the lesson. One of: 'Temel', 'Orta', 'İleri'."
        },
        "category": {
          "type": "string",
          "description": "The specific topic/category of the lesson from the curriculum (e.g., 'Pozlama Temelleri', 'Görsel Hikâye Anlatımı')."
        },
        "title": {
          "type": "string",
          "description": "The title of the lesson."
        },
        "learningObjective": {
          "type": "string",
          "description": "The skill the user will gain from this lesson."
        },
        "theory": {
          "type": "string",
          "description": "The main educational content/theory for the lesson."
        },
        "analysisCriteria": {
          "type": "array",
          "description": "A list of 3 technical criteria for a photo to be considered 'successful' according to this lesson.",
          "items": {
            "type": "string"
          }
        },
        "practiceTask": {
          "type": "string",
          "description": "A specific, actionable shooting task for the user."
        },
        "auroNote": {
          "type": "string",
          "description": "An explanation of why this lesson's analysis provides a professional perspective."
        },
        "imageUrl": {
          "type": "string",
          "description": "The URL of an example photograph associated with the lesson.",
          "format": "uri"
        },
        "imageHint": {
          "type": "string",
          "description": "Keywords used to find a suitable image for the lesson."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the lesson was generated.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "level",
        "category",
        "title",
        "learningObjective",
        "theory",
        "analysisCriteria",
        "practiceTask",
        "auroNote",
        "imageUrl",
        "imageHint",
        "createdAt"
      ]
    },
    "UserLessonProgress": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "UserLessonProgress",
      "type": "object",
      "description": "Tracks a user's progress through the Viewora Academy lessons, recording which lessons they have completed.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the UserLessonProgress entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User making progress. (Relationship: User 1:N UserLessonProgress)"
        },
        "lessonId": {
          "type": "string",
          "description": "Reference to the AcademyLesson being tracked. (Relationship: AcademyLesson 1:N UserLessonProgress)"
        },
        "isCompleted": {
          "type": "boolean",
          "description": "Indicates whether the user has marked this lesson as completed."
        },
        "completedAt": {
          "type": "string",
          "description": "Timestamp when the lesson was marked as completed by the user.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "userId",
        "lessonId",
        "isCompleted"
      ]
    },
    "Transaction": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Transaction",
      "type": "object",
      "description": "Records financial and token-related transactions, such as token purchases, gifts, or token spending for AI analysis.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Transaction entity."
        },
        "userId": {
          "type": "string",
          "description": "Reference to the User involved in the transaction. (Relationship: User 1:N Transaction)"
        },
        "amount": {
          "type": "number",
          "description": "The number of tokens involved in this transaction (positive for gain, negative for spend)."
        },
        "currencyAmount": {
          "type": "number",
          "description": "The monetary amount of the transaction, if applicable (e.g., for purchases)."
        },
        "currency": {
          "type": "string",
          "description": "The currency used for the transaction, if applicable (e.g., 'TL')."
        },
        "type": {
          "type": "string",
          "description": "The type of transaction (e.g., 'Purchase', 'Gift', 'Analysis_Spend')."
        },
        "status": {
          "type": "string",
          "description": "The status of the transaction (e.g., 'Pending', 'Completed', 'Failed')."
        },
        "transactionDate": {
          "type": "string",
          "description": "Timestamp when the transaction occurred.",
          "format": "date-time"
        },
        "paymentProviderTransactionId": {
          "type": "string",
          "description": "External transaction ID from the payment provider (e.g., Iyzico/Stripe), if applicable."
        }
      },
      "required": [
        "id",
        "userId",
        "amount",
        "type",
        "status",
        "transactionDate"
      ]
    },
    "Group": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Group",
      "type": "object",
      "description": "Represents a user-created community, class, or workshop within the Viewora application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Group entity."
        },
        "name": {
          "type": "string",
          "description": "The display name of the group."
        },
        "description": {
          "type": "string",
          "description": "A short description of the group's purpose or theme."
        },
        "ownerId": {
          "type": "string",
          "description": "The UID of the user who created and owns the group."
        },
        "memberIds": {
          "type": "array",
          "description": "A list of user UIDs who are members of this group.",
          "items": {
            "type": "string"
          }
        },
        "maxMembers": {
          "type": "number",
          "description": "The maximum number of members this group can have, based on the owner's level."
        },
        "createdAt": {
          "type": "string",
          "description": "Timestamp when the group was created.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "name",
        "ownerId",
        "memberIds",
        "maxMembers",
        "createdAt"
      ]
    },
    "GroupInvite": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "GroupInvite",
      "type": "object",
      "description": "Represents a pending invitation for a user to join a group.",
      "properties": {
        "id": { "type": "string" },
        "groupId": { "type": "string" },
        "groupName": { "type": "string" },
        "inviterId": { "type": "string" },
        "inviterName": { "type": "string" },
        "inviteeId": { "type": "string", "description": "The UID of the user who is invited. Set after the user accepts or is found." },
        "inviteeEmail": { "type": "string", "format": "email", "description": "The email of the user to invite." },
        "status": { "type": "string", "enum": ["pending", "accepted", "declined"] },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["groupId", "groupName", "inviterId", "inviterName", "inviteeEmail", "status", "createdAt"]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "users/{userId}",
        "definition": {
          "entityName": "User",
          "schema": {
            "$ref": "#/backend/entities/User"
          },
          "description": "Stores individual user profiles including authentication details, token balance, and gamification level. This collection is private, with documents owned by the user matching the '{userId}' wildcard.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user, matching 'request.auth.uid'."
            }
          ]
        }
      },
      {
        "path": "users/{userId}/photos/{photoId}",
        "definition": {
          "entityName": "Photo",
          "schema": {
            "$ref": "#/backend/entities/Photo"
          },
          "description": "A subcollection storing photos submitted by a specific user for AI analysis. Documents are private and owned by the user matching the '{userId}' wildcard. Includes denormalized 'userId' for authorization independence and easier querying.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user who owns these photos."
            },
            {
              "name": "photoId",
              "description": "The unique identifier for a specific photo."
            }
          ]
        }
      },
      {
        "path": "users/{userId}/lessonProgress/{progressId}",
        "definition": {
          "entityName": "UserLessonProgress",
          "schema": {
            "$ref": "#/backend/entities/UserLessonProgress"
          },
          "description": "A subcollection tracking a specific user's progress through the academy lessons. Documents are private and owned by the user matching the '{userId}' wildcard. Includes denormalized 'userId' and 'lessonId' for authorization independence and easier querying.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user whose lesson progress is being tracked."
            },
            {
              "name": "progressId",
              "description": "The unique identifier for a specific user's lesson progress entry."
            }
          ]
        }
      },
      {
        "path": "users/{userId}/transactions/{transactionId}",
        "definition": {
          "entityName": "Transaction",
          "schema": {
            "$ref": "#/backend/entities/Transaction"
          },
          "description": "A subcollection recording all financial and token-related transactions for a specific user. Documents are private and owned by the user matching the '{userId}' wildcard. Includes denormalized 'userId' for authorization independence and easier querying.",
          "params": [
            {
              "name": "userId",
              "description": "The unique identifier of the user involved in the transaction."
            },
            {
              "name": "transactionId",
              "description": "The unique identifier for a specific transaction."
            }
          ]
        }
      },
      {
        "path": "groups/{groupId}",
        "definition": {
          "entityName": "Group",
          "schema": {
            "$ref": "#/backend/entities/Group"
          },
          "description": "Stores group information, including owner and member lists. Access is restricted to members.",
          "params": [
            {
              "name": "groupId",
              "description": "The unique identifier for the group."
            }
          ]
        }
      },
      {
        "path": "groups/{groupId}/photos/{photoId}",
        "definition": {
          "entityName": "Photo",
          "schema": {
            "$ref": "#/backend/entities/Photo"
          },
          "description": "A subcollection for photos shared within a specific group. Access is limited to group members.",
          "params": [
            {
              "name": "groupId",
              "description": "The unique identifier of the group that owns these photos."
            },
            {
              "name": "photoId",
              "description": "The unique identifier for a specific photo within the group."
            }
          ]
        }
      },
       {
        "path": "group_invites/{inviteId}",
        "definition": {
          "entityName": "GroupInvite",
          "schema": { "$ref": "#/backend/entities/GroupInvite" },
          "description": "A global collection of all pending and processed group invitations.",
          "params": [
            { "name": "inviteId", "description": "The unique ID for the group invitation." }
          ]
        }
      },
      {
        "path": "academyLessons/{lessonId}",
        "definition": {
          "entityName": "AcademyLesson",
          "schema": {
            "$ref": "#/backend/entities/AcademyLesson"
          },
          "description": "A top-level collection containing all micro-learning lessons for the Viewora Academy. These documents are public and read-only for all authenticated users.",
          "params": [
            {
              "name": "lessonId",
              "description": "The unique identifier for an academy lesson."
            }
          ]
        }
      },
       {
        "path": "weekly_profile_snapshots/{snapshotId}",
        "definition": {
          "entityName": "WeeklyProfileSnapshot",
          "schema": {
            "type": "object",
            "properties": {
                "user_id": {"type": "string"},
                "profile_score": {"type": "number"},
                "week_number": {"type": "number"}
            }
          },
          "description": "Stores weekly snapshots of user profile scores for tracking progress.",
          "params": [
            {
              "name": "snapshotId",
              "description": "The unique identifier for a weekly snapshot."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore data structure for Viewora AI Coach is designed with a strong emphasis on Authorization Independence, Structural Segregation, and predictable access patterns to ensure security, scalability, and debuggability. The core strategy revolves around path-based authorization and denormalization.\n\n**Authorization Independence:** This is achieved primarily by structuring all user-specific data as subcollections under a top-level `/users/{userId}` collection. For instance, user photos are stored at `/users/{userId}/photos/{photoId}`, user lesson progress at `/users/{userId}/lessonProgress/{progressId}`, and transactions at `/users/{userId}/transactions/{transactionId}`. This design ensures that security rules can directly compare `request.auth.uid` with the `{userId}` wildcard in the path, eliminating the need for expensive and potentially insecure `get()` calls to determine ownership.\n\n**Group Structure:** The new `/groups/{groupId}` collection introduces a community dimension. Access to a group's data is primarily controlled by a `memberIds` array within each group document. This avoids complex joins and allows security rules to efficiently check for membership (`request.auth.uid in resource.data.memberIds`). Group-specific content, like the shared photo gallery at `/groups/{groupId}/photos/{photoId}`, inherits its permissions from the parent group, ensuring only members can view or contribute.\n\n**QAPs (Queries as Authorization Primitives):** The structural segregation directly supports secure `list` operations. Because all documents in a path like `/users/{userId}/photos` inherently belong to the `userId` specified in the path, any `list` query on this path will only return documents that the authenticated user is authorized to access. This same principle applies to groups, where queries on a group's subcollections are protected by membership checks. Similarly, the `academyLessons` collection's top-level, public read-only nature simplifies security for `list` operations, making them accessible to all authenticated users."
  }
}

    