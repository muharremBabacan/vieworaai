// This file is intentionally left blank.
// The root page is now handled by the middleware which redirects to the
// default locale (e.g., /tr). The actual page content is in
// `src/app/[locale]/page.tsx`.
export default function Page() {
  return null;
}
