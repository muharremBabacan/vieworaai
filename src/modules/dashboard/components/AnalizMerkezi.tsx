'use client';

import PhotoAnalyzer from './photo-analyzer/PhotoAnalyzer';

/**
 * AnalizMerkezi (Dashboard Wrapper)
 * Refactored to modular architecture v4.2.0
 */
export default function AnalizMerkezi() {
  return <PhotoAnalyzer />;
}
